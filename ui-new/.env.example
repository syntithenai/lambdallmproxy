# ================================================================
# UI Environment Configuration (Example)
# ================================================================
# Copy this file to .env and configure for your deployment
# ================================================================

# ----------------------------------------------------------------
# API ENDPOINT CONFIGURATION
# ----------------------------------------------------------------

# Base URL for Lambda API endpoint
# This is the primary variable used by the UI (in utils/api.ts)
# 
# Examples:
#   Production Lambda: https://your-function-url.lambda-url.region.on.aws
#   API Gateway: https://your-api-id.execute-api.region.amazonaws.com/prod
#   Local dev: http://localhost:3000 (or leave empty for same-origin)
#   Custom domain: https://api.yourdomain.com
# 
# IMPORTANT: 
#   - Do NOT include trailing slash
#   - Rebuild required after changing (npm run build)
#   - In development mode, restart dev server (npm run dev)
VITE_API_BASE=https://your-lambda-url.lambda-url.us-east-1.on.aws

# Public URL for the UI application (used for share links)
# This should ALWAYS point to your production URL, even in development
# Used for generating shareable links that work from anywhere
# 
# Examples:
#   Production: https://ai.syntithenai.com
#   Custom domain: https://app.yourdomain.com
# 
# IMPORTANT:
#   - Do NOT include trailing slash
#   - Should be production URL even when developing locally
VITE_PUBLIC_URL=https://ai.syntithenai.com

# ----------------------------------------------------------------
# OPTIONAL CONFIGURATION
# ----------------------------------------------------------------

# Legacy Lambda URL (used in App.tsx as fallback)
# Can be same as VITE_API_BASE
# VITE_LAMBDA_URL=https://your-lambda-url.lambda-url.us-east-1.on.aws

# Google Client ID for OAuth authentication
# Required for Google Sign-In, Drive sync, and Docs/Sheets integration
# Get from: https://console.cloud.google.com/apis/credentials
# VITE_GGL_CID=your-client-id.apps.googleusercontent.com

# ================================================================
# DEPLOYMENT NOTES
# ================================================================
# 
# Production Build:
#   1. Set VITE_API_BASE to your Lambda URL
#   2. Run: npm run build
#   3. Deploy dist/ folder to hosting service
# 
# Development:
#   1. Set VITE_API_BASE (or leave empty for same-origin)
#   2. Run: npm run dev
#   3. Vite will auto-reload on .env changes
# 
# Environment Variables in Vite:
#   - Only variables prefixed with VITE_ are exposed to client
#   - Access via: import.meta.env.VITE_VARIABLE_NAME
#   - Never store sensitive keys in UI environment variables
# 
# ================================================================
